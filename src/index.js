import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// import 'src/MS Sans Serif 8pt.ttf';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);